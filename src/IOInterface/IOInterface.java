package IOInterface;

public class IOInterface {
    // OrderOperation orderOperation = OrderOperation.getInstance();
    //        OrderListResult orderListResult = orderOperation.getOrderList("u_0000000002", 1);
    //        for (int i = 0; i < orderListResult.getOrders().size(); i++) {
    //            System.out.println(i + 1 + ". " + orderListResult.getOrders().get(i).toString());
    //        }
}
